# Controls when the workflow will run
on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: build app
        run: sh ./deploy.sh
        shell: pwsh
